# Jetson TX2 vehicle-side dependencies only.
# Video runs through gst-launch and NVIDIA GStreamer plugins directly.  Do not
# install opencv-python for this uploader; it isn't required and can disturb the
# multimedia packages supplied by JetPack.

websockets>=9.1,<10.0; python_version < "3.7"
websockets>=10.0,<12.0; python_version >= "3.7"
dataclasses==0.8; python_version < "3.7"
pyserial>=3.4,<4.0
